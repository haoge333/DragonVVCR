<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dragonvvcr.mapper.DictionaryTypeMapper">

    <select id="selectDictionaryTypePage" resultType="com.dragonvvcr.entity.DictionaryType">
        SELECT
            id, type_code, type_name, status, remark, create_time, update_time, create_by, update_by
        FROM
            dictionary_type
        <where>
            <if test="typeCode != null and typeCode != ''">
                AND type_code LIKE CONCAT('%', #{typeCode}, '%')
            </if>
            <if test="typeName != null and typeName != ''">
                AND type_name LIKE CONCAT('%', #{typeName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
